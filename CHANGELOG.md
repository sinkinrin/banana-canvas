# Changelog

本项目的用户可见变更记录在此文件中。版本号遵循 Semantic Versioning。

## [Unreleased]

## [0.10.0] - 2026-09-23

### 新增

- 节点颜色升级为项目内分类：创作和图片节点可归类、命名，同色共用名称；画布分类面板显示数量并一键定位，缩略地图同步显示分类色。
- 分类随项目保存，支持撤销与重做；普通生成、对比生成、局部编辑、抠图和作为参考图创建的新节点继承来源分类。

### 改进与修复

- 创作节点的对比、生成和取消操作移到设置上方，展开设置不再把按钮推走；基础参数优先展示，Image2 / Image 2.5 / Banana 高级参数默认折叠，保留已选值并提示自定义配置。
- 应用设置的保存与取消、软件更新操作、对比生成和局部编辑生成按钮脱离滚动内容，短窗口下仍可操作；局部编辑结果对比限制高度，图片区域独立滚动。
- 多模型对比设置弹窗收窄，记住上次启动对比时的模型、比例和分辨率；首次使用沿用当前创作节点参数，不兼容参数回退到共同支持的值。
- 对比生成后直接回到画布，不再弹出结果窗口；图片下方保留“查看对比”并改为定位同组结果，最佳方案直接在结果节点标记。
- 生成中的每张卡片增加“停止此任务”，单独取消普通/对比生成或局部编辑；重新生成也可单独停止。创作节点保留“停止全部未完成任务”，已完成结果不受影响。

### 安装提示

- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。后台下载完成后仍由用户确认是否立即重启安装。

## [0.9.4] - 2026-09-22

### 修复

- 大项目打开改为分别读取项目结构和图片，逐张校验，避免图片合并成超大 JSON 导致加载失败，并降低临时内存占用。
- 旧项目迁移改为逐图上传与轻量快照提交，支持失败后继续迁移；主动删除全部项目后不再重新导入遗留画布。
- 移除旧画布重复的 IndexedDB 图片镜像写入，保留项目级存储和旧数据迁移。
- 保存失败可通过“重试保存”恢复；删除项目时协调在途保存，避免残留保存任务阻塞其他项目打开或应用退出。
- 已有项目文件缺失或损坏时停止加载和写入，保护剩余图片，避免将损坏项目当作空画布覆盖。
- 生图超时保留失败节点并显示超时提示；修复连续粘贴参考图相互覆盖，蒙版任务在删除节点或退出项目时取消。
- 将 qs 更新至 6.16.0，修复相关依赖安全告警。

### 改进

- 参考图缩略图左上角改为单击放大查看，移除该处的智能抠图和局部编辑菜单；右键缩略图可直接复制图片并显示结果。
- 大图查看工具栏在下载按钮右侧提供局部编辑入口，支持参考图和生成图。
- 增加迁移重试、保存与删除竞争、损坏数据保护及图片交互的回归验证。

### 已知限制

- 大项目仍会将全部图片载入内存；图片按需加载与大画布渲染优化留待后续版本。
- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。

## [0.9.3] - 2026-09-22

### 修复

- 修复相机 MPO 多图像 JPEG 可预览但 Image2 在数秒内拒绝的问题：发送时无损提取主图，移除多图像索引和附加预览图，保留主图压缩数据、方向与颜色信息。
- 统一参考图兼容处理，覆盖多图生成、重新生成、多模型对比和局部编辑；按真实文件格式修正 MIME，特殊颜色模式 JPEG、16 位/索引 PNG、GIF、动画 WebP/APNG 及可解码 AVIF 转为单帧 PNG，保留透明度与显示方向。HEIC/HEIF 在设备支持解码时转换，否则明确提示重新导出。
- 保留画布中的原始文件，转换副本仍遵守单张 16 MiB、合计 40 MiB 限制；提示多帧图片使用主图或首帧。
- 上游返回 invalid_image_file 时显示具体参考图格式错误，不再只显示通用请求 ID；不向界面转发上游私密内容。
- 增加 MPO 无损提取、转换错误与取消检查，以及真实 Electron 上传、透明度、CMYK 颜色/EXIF 方向、16 位 PNG、原图保存和错误展示回归验证。
- 将间接依赖 adm-zip 更新至 0.6.1，修复发布审计发现的高危内存分配漏洞。

### 已知限制

- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。

## [0.9.2] - 2026-09-16

### 修复

- 修复生成中的任务拖到画布边缘后，自动平移使源节点离开视野，导致请求被取消、任务卡片被删除的问题。
- 平移、缩放和拖动到屏幕外时保留节点及其生成、重新生成和编辑状态；主动取消、删除源节点和离开项目时仍执行原有清理。
- 新增真实 Electron 拖动回归检查，覆盖屏幕外生成完成、结果保存重开、主动取消、删除源节点和离开项目。

### 已知限制

- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。

## [0.9.1] - 2026-09-15

### 修复

- 修复点击关闭后窗口消失、后台进程仍驻留，导致再次点击快捷方式无法打开的问题；本地服务清理后延后到下一轮事件循环完成退出。
- 保留退出前等待项目保存的流程，并补充真实关闭、进程退出、重新启动和未提交草稿恢复的回归检查。

### 改进

- 桌面版改为无边框窗口：顶部拖动区与界面同色，提供中英文最小化、最大化/还原和关闭按钮，保留边缘缩放及系统阴影。
- 画布和弹窗适配自定义顶部区域，最小窗口下仍可使用窗口控制；浏览器版布局不变。

### 已知限制

- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。

## [0.9.0] - 2026-09-15

### 新增

- 桌面版默认在每次启动后检查更新，发现新版本时弹窗提示；可在“应用设置 → 软件更新”关闭并记住选择。该检查默认不下载或安装，无更新或检查失败时不弹窗。
- 新增多模型对比生成：同一提示词与参考图，每个所选模型生成一张，结果并排展示；支持同步缩放、拖动和最佳方案标记，保存重开后可继续查看。
- 新增结果信息卡：展示图片实际尺寸与格式、生成耗时、请求模型、服务器回报型号，以及请求与回报画质；生成记录随项目保存。
- 模型连接设置新增“服务器支持的模型”，按标准 `/v1/models` 查询，支持刷新、搜索及连接错误提示；Key 始终留在后端。

### 已知限制

- 对比中的每个模型各发起一次请求，可能分别计费；需要分别配置相应提供方的访问权限。
- 服务器模型列表仅代表该连接公布的模型，不保证全部支持图片生成；信息卡中未返回的历史/服务器字段显示“未记录”，不推断模型身份或收费。
- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。

## [0.8.0] - 2026-09-15

### 新增

- 创作节点新增 Image 2.5 Flare / Sunburst，共用现有 Image2 连接；参考图、蒙版编辑、重试与项目持久化保留所选型号。
- Image 2.5 新增透明背景和自动背景选择；可请求 PNG/WebP，JPEG 自动转为 PNG，并使用棋盘格预览；后端已实测返回透明 PNG。
- 前端中英文说明和项目介绍补充 2.5 两款模型的定位，以及中转实测能力边界。

### 已知限制

- 当前中转对 `xhigh` / `max` 请求回报 `low` / `medium`，这两档暂未开放；`high` 也出现回报 `medium` 的情况。请求尺寸不保证等于实际图片尺寸，流式请求尚未实测到局部图事件。
- 当前中转对 WebP 请求仍返回 PNG；项目按实际图片数据识别格式。
- Windows 安装包目前未签名，安装或更新时可能出现 SmartScreen“未知发布者”提示。

## [0.7.0] - 2026-09-09

### 新增

- 新增本地智能抠图：桌面版内置 IS-Net INT8，图片和参考图都可从“图片工具”菜单处理。
- 抠图结果作为独立图片节点生成，保留原图、原尺寸和透明 PNG；支持下载、复制、继续作为参考图和项目重载。
- 设置新增“智能抠图”页，可下载、校验、切换和删除 IS-Net FP32、BiRefNet Lite FP32；内置模型始终可离线使用。
- 新增透明棋盘格、浅色和深色背景预览，以及抠图取消、超时和失败提示。

### 改进

- 抠图推理放在 Electron 独立后台进程，避免阻塞画布；下载模型使用临时文件、长度校验和 SHA256 校验后原子安装。
- 补充模型来源、许可、量化转换说明和真实 Electron 离线/打包 smoke 验证。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。
- 默认 INT8 是权重量化版，激活和运算保持 FP32；CPU 速度和边缘质量仍取决于图片与设备。

## [0.6.2] - 2026-09-07

### 修复

- 修复提示词编辑弹窗中长内容或小窗口导致保存、取消和关闭按钮被裁切的问题；弹窗适配窗口高度，操作按钮保持可见。
- 提示词输入框随内容自动增高或缩回，达到高度上限后支持滚动查看。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。

## [0.6.1] - 2026-09-07

### 修复

- 修复快速切换项目时，旧项目的迟到加载结果覆盖当前画布并可能保存到错误项目的问题。
- 编辑后立即显示待保存状态；桌面端退出、刷新或重启安装更新前等待保存完成，保存失败时保留窗口。浏览器存在未保存修改时会显示离开提醒。
- Image2 的超时和取消现在覆盖完整响应读取及生成图片下载，避免收到响应头后请求继续挂起。
- 修复 IPv4 映射 IPv6 地址绕过生成图片下载的本机与私有网络访问限制的问题。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。

## [0.6.0] - 2026-09-03

### 新增

- 应用界面新增 English / 简体中文切换，首次启动跟随系统语言，并在本机记住用户选择。

### 改进

- 将界面文案集中到类型一致的 i18n 资源，覆盖项目、画布、节点、提示词库、设置、更新、蒙版与构图草图流程。
- 提示词优化默认改用正式版 `gemini-3.8-flash`，并支持在应用设置或 `.env` 中自定义模型。

### 文档

- 默认 README 改为精简英文首页并高亮中文入口；新增 `README_CN.md`，把配置与开发细节拆分到独立文档。

## [0.5.1] - 2026-09-03

### 改进

- 参考图单张上限由 10 MiB 提高到 16 MiB，图片合计上限由 32 MiB 提高到 40 MiB，并将本地 JSON 请求体上限配套提高到 64 MiB。
- Windows 桌面端移除默认 Electron 菜单栏，让应用界面保持简洁一致。

### 修复

- 上传或粘贴参考图时会在读取前检查体积并显示文件名、实际大小和允许上限；服务端也会准确区分图片过大与 Base64 数据无效，不再显示误导性错误。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。

## [0.5.0] - 2026-09-02

### 新增

- 设置中新增 Banana / Gemini 独立代理开关与代理 URL；默认直连，开启后同时用于 Banana 系列生图和 Gemini 提示词优化，不影响 Image2 的代理模式。
- 新增跨项目提示词库，支持标题、内容、标签、搜索、新建、编辑、复制、删除、收藏当前提示词，以及一键填入或创建创作节点。
- 设置新增“软件更新”页，可手动检查线上最新版本、查看更新日志、下载进度并主动重启安装；自动更新改为默认关闭，由用户显式开启。

### 修复

- 修复生成结果“重新生成”点击后缺少可见响应的问题；结果动作统一隔离 React Flow 拖拽手势，并为重新生成和复制失败补充明确状态反馈。

### 发布

- Windows 打包阶段会把当前版本 Changelog 和未签名提示写入 `latest.yml`，Release 门禁会拒绝缺少客户端更新日志的元数据。

## [0.4.0] - 2026-09-02

### 新增

- 生图模型选择新增 Banana 2 Lite（`gemini-3.1-flash-lite-image`）与 Banana Pro（`gemini-3-pro-image`），并在生成结果、重新生成和“以此为参考”链路中保留所选变体。
- 为三个 Banana 变体加入统一能力矩阵，按模型动态提供画幅、输出尺寸、思考等级、参考图解析和 Google Search 选项。

### 改进

- 修复 Windows 桌面端生成图片无法复制的问题：通过隔离的 Electron 原生剪贴板桥接写入 JPEG/PNG 等图片，并在失败时给出可见提示。
- Banana 2 Lite 固定输出 1K，但开放官方支持的 14 种比例与 `MINIMAL` / `HIGH` 思考等级，并自动过滤不支持的 Google Search。
- Banana Pro 提供 1K、2K、4K、10 种标准比例与 Google Search；思考由模型管理，并自动过滤会触发 API 400 的 `mediaResolution`。
- 模型切换、前端请求和服务端校验都会规范化不兼容的旧节点参数；Banana Pro 请求超时提高到 5 分钟。
- 使用 Gemini Models API 与实际生图请求验证三个稳定模型；Lite 的极端画幅/高思考、Lite/常规版的参考图解析及 Pro 的参考图生成均通过。

### 调研

- 评估 Google 已全面推出的 Interactions API。它的服务端多轮状态、执行步骤和后台任务对连续编辑与 Pro 长任务有价值，但当前存在 SDK 大版本迁移、默认云端存储、自定义安全设置缺失及 Lite 未列入支持表等差异，本版继续使用官方仍完整支持的 `generateContent`。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。

## [0.3.1] - 2026-09-02

### 改进

- Banana 提供方从 Nano Banana 2 预览模型迁移到官方稳定模型 `gemini-3.1-flash-image`。
- 同步更新模型请求断言与用户文档，保留现有分辨率、宽高比、思考模式和搜索增强参数。

### 发布

- 这是内置更新器的 v0.3.0 客户端可自动接收的首个增量版本，继续通过 GitHub Releases 提供安装包、blockmap 与 `latest.yml`。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。

## [0.3.0] - 2026-09-02

### 新增

- 新增 QuickDraw 构图草图编辑器，可按当前画幅绘制人物位置、动作和画面关系。
- 草图以固定画幅 PNG 作为生成参考图，同时保存可继续编辑的 QuickDraw 快照。
- Windows 客户端会自动从 GitHub Releases 检查并后台下载稳定更新；下载完成后由用户选择立即重启或稍后安装。
- 项目列表和画布标题显示当前客户端版本。

### 改进

- 同一创作节点再次应用草图时原位替换旧草图参考，不重复占用参考图名额。
- Electron smoke test 覆盖草图绘制、应用为参考图和重新打开编辑。
- 升级存在安全公告的传递依赖，并保持 npm audit 为零漏洞。

### 发布

- 新增版本号、README、lockfile 和 Changelog 一致性门禁。
- 新增 tag 驱动的 Windows 构建、已打包程序 smoke、更新元数据校验、SHA256 清单和 GitHub Release 发布流程。
- GitHub Release 同时发布 NSIS 安装包、blockmap、`latest.yml` 和 `SHA256SUMS.txt`。

### 已知限制

- Windows 安装包暂未进行代码签名，安装和自动更新时可能出现 SmartScreen 未知发布者提示。
- v0.2.1 不包含更新器，需要手动安装一次 v0.3.0；从 v0.3.0 开始才能接收后续自动更新。

## [0.2.1] - 2026-07-23

### 新增

- 新增 Electron Windows 桌面版与 NSIS 安装支持。
- 新增应用内模型连接设置、运行时热更新和系统安全存储。
- 加入 CI、ESLint、Electron smoke test 与 Windows 构建脚本。

### 改进

- 改进 Image2 请求、代理、取消、校验与生成结果处理。
- 增强本地项目资产持久化与可恢复删除。

[Unreleased]: https://github.com/sinkinrin/banana-canvas/compare/v0.7.0...HEAD
[0.7.0]: https://github.com/sinkinrin/banana-canvas/compare/v0.6.2...v0.7.0
[0.6.0]: https://github.com/sinkinrin/banana-canvas/compare/v0.5.1...v0.6.0
[0.5.1]: https://github.com/sinkinrin/banana-canvas/compare/v0.5.0...v0.5.1
[0.5.0]: https://github.com/sinkinrin/banana-canvas/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/sinkinrin/banana-canvas/compare/v0.3.1...v0.4.0
[0.3.1]: https://github.com/sinkinrin/banana-canvas/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/sinkinrin/banana-canvas/compare/v0.2.1...v0.3.0
[0.2.1]: https://github.com/sinkinrin/banana-canvas/releases/tag/v0.2.1
